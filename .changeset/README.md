# Changesets

Changesets ile versiyon yönetimi yapılır. Yeni bir değişiklik eklemek için:

```bash
pnpm changeset
```

Versiyonu güncellemek için:

```bash
pnpm changeset:version
```

Yayınlamak için:

```bash
pnpm changeset:publish
```
